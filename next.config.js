/** @type {import('next').NextConfig} */
const nextConfig = {
    output:'export',
    images: {
        domains: ['superbikescentral.online', 'cdn.imagin.studio'],
    }
}

module.exports = nextConfig
